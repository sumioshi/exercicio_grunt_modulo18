// Arquivo de exemplo para minificação

function soma(a, b) {
    return a + b;
}

function subtracao(a, b) {
    return a - b;
}

console.log(soma(5, 3)); // Saída esperada: 8
console.log(subtracao(10, 4)); // Saída esperada: 6
